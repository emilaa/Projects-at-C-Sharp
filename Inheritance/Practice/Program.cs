using Domain.Models;
using Practice.Models;
using System;
using System.Reflection;

namespace Practice
{
    class Program
    {
        static void Main(string[] args)
        {
            //Person person = new Person();
            //person.name = "Emil";


            //typeof(Person).GetField("surname", BindingFlags.NonPublic | BindingFlags.Instance).SetValue(person, "Abdullayev");

            //var personSurname = typeof(Person).GetField("surname", BindingFlags.NonPublic | BindingFlags.Instance).GetValue(person);

            //Console.WriteLine(personSurname);

            //Car car = new Car();
            //car.HorsePower = 50;
            //Console.WriteLine(car.HorsePower);

            //Animal animal = new Animal

            Book book = new Book();
                
        }
    }
}
